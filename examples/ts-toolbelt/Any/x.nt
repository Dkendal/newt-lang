// TODO: the original TS uses a value-level unique symbol:
//   const _ = Symbol('x')
//   export type x = typeof _ & {}
// newtype has no value declarations, `typeof`, or unique symbols, so the
// closest expressible placeholder is `symbol & {}`.
export type x do symbol & {} end
