// Check whether `U` contains `U1`
export type Has(U, U1)
    where
        U <: any,
        U1 <: any
    as
        if [U1] <: [U] then
            1
        else
            0
        end

unittest "Has(U, U1)" do
    assert Has('a' | 'b', 'a') == 1
    assert Has('a' | 'b', 'c') == 0
end
