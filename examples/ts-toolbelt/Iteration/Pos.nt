// Ported from ts-toolbelt sources/Iteration/Pos.ts

import { Iteration } from "./Iteration"

// Get the position of `I` (**number**)
export type Pos(I)
    where I <: Iteration
    do I[0]
end

// TODO allow for types in unittest blocks that aren't emitted
type I20 do [20, '+', '19', '21', '-20'] end

unittest "Pos(I)" do
    assert Pos(I20) == 20
end
