import { Length } from "./Length"
import { List } from "./List"

export type Head(L)
    where L <: List
    as
        if Length(L) <: 0 then
            never
        else
            L[0]
        end

unittest "Head(L)" do
    assert Head([1, 2, 3]) == 1
    assert Head([]) == never
end
