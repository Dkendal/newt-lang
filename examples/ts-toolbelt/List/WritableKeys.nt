import { WritableKeys as OWritableKeys } from "../Object/WritableKeys"
import { ObjectOf } from "./ObjectOf"
import { List } from "./List"

// Get the keys of `L` that are writable
export type WritableKeys(L)
    where L <: List
    do OWritableKeys(ObjectOf(L))
end
