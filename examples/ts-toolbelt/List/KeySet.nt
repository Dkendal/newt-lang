import { Range } from "../Number/Range"
import { UnionOf } from "./UnionOf"

export type KeySet(From, To)
    where
        From <: number,
        To <: number
    do UnionOf(Range(From, To, '->'))
end
